apiVersion: v1
kind: Service
metadata:
  name: wbgt-frontend
  labels:
    app.kubernetes.io/name: wbgt-frontend
    app.kubernetes.io/part-of: wbgt
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wbgt-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
