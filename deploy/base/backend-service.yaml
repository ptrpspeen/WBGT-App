apiVersion: v1
kind: Service
metadata:
  name: wbgt-backend
  labels:
    app.kubernetes.io/name: wbgt-backend
    app.kubernetes.io/part-of: wbgt
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wbgt-backend
  ports:
    - name: http
      port: 10000
      targetPort: http
