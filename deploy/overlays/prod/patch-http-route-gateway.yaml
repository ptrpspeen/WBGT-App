apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: wbgt
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: production-gateway-01
      namespace: default
